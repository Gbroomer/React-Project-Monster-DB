import React, { useEffect, useState } from "react"
// import filteredMonsters from "./filteredMonsters"

function MonsterList({ monster, eachMonster }) {
    

    // const allMonsters= monsters.map((monster) => )
    // const monsters1 = monsters.map(obj => {
        
    //     const { index, name, url } = obj;
    //     console.log(obj.name)
    //     return { index, name, url }
    // })

    // console.log(monsters[0].name)
    const BMonsters = [];
    const CMonsters = [];
    const DMonsters = [];
    const EMonsters = [];
    const FMonsters = [];
    const GMonsters = [];
    const HMonsters = [];
    const IMonsters = [];
    const JMonsters = [];
    const KMonsters = [];
    const LMonsters = [];
    const MMonsters = [];
    const NMonsters = [];
    const OMonsters = [];
    const PMonsters = [];
    const QMonsters = [];
    const RMonsters = [];
    const SMonsters = [];
    const TMonsters = [];
    const UMonsters = [];
    const VMonsters = [];
    const WMonsters = [];
    const XMonsters = [];
    const YMonsters = [];
    const ZMonsters = [];

    return (
        <div>
           {monster} 
        </div>
        
    )
}

export default MonsterList;